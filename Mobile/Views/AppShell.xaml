<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RodentTribe.Views.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:RodentTribe.Views"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Выбор шкафа"
        BindingContext="{Binding ClosetsViewModel}"
        ContentTemplate="{DataTemplate views:ClosetSelectView}"
        Route="MainPage"
        Shell.BackgroundColor="WhiteSmoke"
        Shell.TitleColor="Black" />
</Shell>
